/**
 * @author sunlei11@baidu.com.
 * @description
 * 2014/7/31
 */
'use strict';

var dbUtil = require("../index");

dbUtil.init({
    host: "localhost",
    user: "sarike",
    password: "test",
    database: "dbutil"
});

var user = {
    username: 'sunlei',
    password: 'test',
    email: 'sarike@test.com'
};

dbUtil.exec("insert into user set ?", user, function (result) {
    console.info(result);
});

dbUtil.exec("select * from user", function(result) {
    console.info(result);
});